[ Successes ]
- 


[ Changed ]
- Changed LinkedList to ArrayList in `Block` 
- Implemented `values()` method in `Block` 
- Implemented `copyBlock()` method in `Block` 
- 


[ Difficulties ]
- 


[ To-Do ]
- (x) Start with the game already 
- (/) Implement all the blocks of Tet*** in `Block` 
- (DEFUNCT) Implement return of the ArrayList as an 
    immutable instance via `List.of()` for true 
    immutability 
- (/) Implement copyBlock() in `Block` with parameter 
    "int index" so it knows which block in the List to 
    copy. Output will be Cell[][] 
- 

