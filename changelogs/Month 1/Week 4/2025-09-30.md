[ Successes ]
- Added `BlockManager` 
- Added `BlockCollision` 
- 


[ Changed ]
- `BlockCollision`, unlike its ancenstor, is now instance 
    based. 
- `BlockManager` will now be a state-machine rather than a 
    glorified helper class like its ancestor. 
- 


[ Difficulties ]
- Reading my old code is surprisinglu hard. It's more tangled 
    than I originally have thought. 
- 


[ To-Do ]
- (?) Changed `buffer` field to something more into context 
- (x) Complete `BlockManager` and its methods. 
- (x) Separate `moveBlock()` into its separate methods, such 
    as `moveLeft()`, `moveRight()`, `moveDown()`. 
- 

