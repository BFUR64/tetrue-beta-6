[ Successes ]
- Added `copy()` on `BlockData`
- 


[ Changed ]
- Removed very old comments that do not reflect the 
    actual state of the codebase. I *may* update 
    some of the comments to better reflect what the 
    class / method actually does. 
- `isHighlight()` has become fully redundant and 
    thus deprecated thanks to the new dual-grid 
    approach of `GridData`. 
- 


[ Difficulties ]
- Shit happened and I took a long break. Man. Sucks 
    but shit happens.
- Trying to decipher what my old self did is quite 
    difficult it turns out. As far as I know, here 
    is the current setup:
    1. `GridData` stores two grids - namely, the 
        actual grid of blocks, and the preview 
        grid for something like ghost blocks. 
    2. I'm supposed to pass each one individually 
        to `Renderer` as far as I am aware.
- Reading `GridManager`... It appears that what I 
    was originally going for was to make GridManger 
    be a high-level layer, a sort of API for 
    GameLoop to interact with, with having methods 
    such as `dropBlock()`, `moveBlockDown()`, etc. 
    It is still missing a few methods, namely the 
    left and right directions, and rotation. 
- 


[ To-Do ]
- 

