[ Successes ]
- Added static-based `Collision`
- Added `EMPTY_BLOCK` in `BlockTemplate` 
- Added `BlockData` 
- Added the following public methods in `BlockData`:
    - `moveDown()` 
    - `moveLeft()` 
    - `moveRight()` 
    - `rotateLeft()` 
    - `rotateRight()` 
    - `revertPosition()` 
    - `revertBlockRotation()` 
    - `getRotatedBlockCopy()` 
- 


[ Changed ]
- `Collision` is instance-based again 
- Changed `isColliding()` to `isValid()` in `Collision`
- Replaced `GridManager` with `GridData` in `isValid()` 
    of `Collision`
- Removed `BlockManager` 
- Changed block rotation logic to be lazy and return a new 
    rotated copy on demand rather than rotating everytime 
    when it is not being called for copying to do stuff to it. 
    It also helps in separation via encapsulation and deep 
    copy of the rotated block 
- Removed `EMPTY_BLOCK` in `BlockTemplate` 
- Flipped the boolean return statements of `isValid()` in 
    `Collision` to reflect the new name 
- 


[ Difficulties ]
- Rotation was slightly difficult... Well, more like, the new 
    implementation of rotation. The old one was conceptually 
    simple but more expensive in terms of memory and 
    calculation. Now it simply just rotates an imaginar enum 
    `Direction` then actually returning a rotated block copy 
    when it is needed rather than always rotating the array 
- 


[ To-Do ]
- (x) Try the implementation of 2025-10-01 in the Difficulties 
    section 
- 

