[ Successes ]
- Added comments for what to do in `BlockManager` 
    hypothetically 
- Added skeleton methods for `BlockManager` 
- Adding `GridManager` class skeleton to test feasability 
- Adding `BlockData` class skeleton 
- 


[ Changed ]
- 


[ Difficulties ]
- Judging the correct direction for the game is super hard. 
    First time I have to make skeleton classes/methods for 
    my project. Idea is GridManager as a sort of API or 
    whatever for my GameLoop. BlockManager knows wayyy too 
    much. It is more a 'GridManager' than managing its block 
    state 
- Right. It is *very* difficult no doubt. I have an initial 
    proposal to break down separation of concerns: 
        1. GridManager 
            - The orchestrator for all of these new classes, 
                not 100% sure what methods it should have yet, 
                but it will reveal itself as I complet the 
                classes below this list 
        2. GridData
            - Stores the internal state of the Grid. Just a 
                dumb class. Reuse what you currently have 
        3. BlockData 
            - Methods: 
                - moveDown() 
                - moveLeft() 
                - moveRight() 
                - ... Methods to expose internal fields 
                    that BlockCollision needs to function  
            - This should have the ability to update its 
                internal positions and block state without 
                worry, but also having the ability to revert 
                when needed via a method call. It duplicates 
                references, needing a previous state, but it 
                is simpler to implement and allows GridManager 
                to flexibly use BlockData instead of 
                micromanaging it to hell 
        4. BlockCollision (static)
            - Methods: 
                - isValid(BlockData, GridData) 
                - isOutOfBounds() 
                - isColliding()
            - Make this static again. Pass arguments manually 
                instead of instancing it. This removes 
                dependency on worrying where to place it. It 
                also means GridManager shall do the 
                orchestration to make it function properly 


[ To-Do ]
- (?) Changed `buffer` field to something more into context 
- (DEFUNCT) Complete `BlockManager` and its methods. 
- (DEFUNCT) Separate `moveBlock()` into its separate methods, such 
    as `moveLeft()`, `moveRight()`, `moveDown()`. 
- 

