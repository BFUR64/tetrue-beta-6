[ Successes ]
- Added initial capacity for `BLOCK_SET` via the static 
    field `BLOCK_SET_CAPACITY` 
- 


[ Changed ]
- Renamed `BLOCKS` to `BLOCK_SET` 
- Renamed `cellmap` to `templates` 
- Renamed `cellgrid` to `grid` 
- Renamed `Block` to `BlockTemplate` 
- Renamed `Cell` to `CellTemplate` 
- 


[ Difficulties ]
- `cellgrid` seems to be a too general term to umbrella 
    classes like `Block` (should be BlockManager) and the 
    `gridData` to be together. It does not seem right, 
    especially when you take in the collision calculations, 
    which is more of an engine thing than a cellgrid thing? 
- 


[ To-Do ]
- Changed `buffer` field to something more into context 
- 

